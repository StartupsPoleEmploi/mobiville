
import styled from 'styled-components'

import { COLOR_PRIMARY } from '../../../constants/colors'

const GuideStepContentTitle = styled.p`
  margin: 0 0 16px 0;

  color: ${COLOR_PRIMARY};
  font-size: 24px;
  font-weight: 900;
`

export default GuideStepContentTitle